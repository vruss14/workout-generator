import React from "react";

// The Log In page

function LoginForm() {

    // Temporary styling to ensure animation works as expected

    const style = {
        textAlign: "center",
        fontWeight: "bold",
        fontSize: "64px",
        marginTop: "3rem"
    }

  return (
      <h1 style={style}>Log In</h1>
  );
}

export default LoginForm;